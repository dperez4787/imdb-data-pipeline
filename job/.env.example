# Copy to .env (gitignored). Never commit real credentials.
# Local runs MUST use MONGODB_DB=imdb_test, never the live "imdb" database.
MONGODB_URI=mongodb+srv://imdb-pipeline:<password>@cluster0.mvdikgq.mongodb.net/imdb?retryWrites=true&w=majority&appName=Cluster0
MONGODB_DB=imdb_test
# Optional: comma-separated collection names to import (default: all 7)
# DATASETS=title_ratings
